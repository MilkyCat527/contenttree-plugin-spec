# Pinned install set for the conformance suite (`pytest` + schema/OpenAPI
# validation only — no SDK, no host/plugin runtime code). Mirrors the
# dependency ranges declared in pyproject.toml. Install with:
#
#   pip install -r requirements.txt
#
pytest>=7.4,<9
jsonschema>=4.20,<5
referencing>=0.31,<1
PyYAML>=6.0,<7
rfc3339-validator>=0.1.4,<1
rfc3987>=1.3.8,<2
